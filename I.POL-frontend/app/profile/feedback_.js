import { View, Text } from "react-native";
import React from "react";
import { Feedback } from "../../components";

const feedback_ = () => {
  return (
    <View>
      <Feedback />
    </View>
  );
};

export default feedback_;
