import { View, Text } from "react-native";
import React from "react";
import { PackageInfo } from "../../../components";

const packageInfo_ = () => {
  return (
    <View>
      <PackageInfo />
    </View>
  );
};

export default packageInfo_;
