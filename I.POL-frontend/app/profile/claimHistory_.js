import { View, Text } from "react-native";
import React from "react";
import { ClaimHistory } from "../../components";

const claimHistory_ = () => {
  return (
    <View>
      <ClaimHistory />
    </View>
  );
};

export default claimHistory_;
