import { View, Text } from "react-native";
import React from "react";
import { SecuritySettings } from "../../components";

const securitySettings_ = () => {
  return (
    <View>
      <SecuritySettings />
    </View>
  );
};

export default securitySettings_;
