import { View, Text } from "react-native";
import React from "react";
import { SpecialPackage } from "../../components";

const specialPackage_ = () => {
  return (
    <View>
      <SpecialPackage />
    </View>
  );
};

export default specialPackage_;
